plugins{
    id 'java-gradle-plugin'
    id 'groovy'
    id 'ivy-publish'
    id 'com.gradle.plugin-publish' version '0.11.0'
}

apply plugin: 'nrlssc.gradle'
sourceCompatibility = 1.8
compileJava.options.encoding = 'UTF-8'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'mil.navy.nrlssc:gplugin-commons:1.8.18'

    testImplementation gradleTestKit(),
            'junit:junit:4.12'
}

hgit{
    forceGit = true
}

pluginBundle {
    website = 'https://geoint.nrlssc.org/'
    vcsUrl = 'https://github.com/seanrl/nrl-gradle/pub-plugin.git'
    tags = ['publish']
}

gradlePlugin {
    //noinspection GroovyAssignabilityCheck
    plugins {
        pubPlugin {
            id = 'nrlssc.pub'
            displayName = 'Simplfied Publish Plugin'
            description = 'Configures boilerplate for Publishing to Artifactory and/or Nexus'
            implementationClass = 'nrlssc.gradle.PubPlugin'
        }
    }
}

